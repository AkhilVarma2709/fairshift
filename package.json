{
  "name": "fairshift-root",
  "private": true,
  "scripts": {
    "install:frontend": "npm --prefix frontend install",
    "build": "NITRO_PRESET=vercel npm --prefix frontend run build && rm -rf .vercel/output && mkdir -p .vercel && cp -R frontend/.vercel/output .vercel/output"
  }
}
